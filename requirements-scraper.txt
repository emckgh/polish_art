# Minimal dependencies for the auction scraper (Linux box / cron runner).
# Does NOT include CV pipeline (torch, CLIP, etc.) or API server deps.
beautifulsoup4==4.12.2
requests==2.31.0
Pillow>=12.0.0
imagehash>=4.3.2
sqlalchemy==2.0.23
python-dotenv==1.0.0
